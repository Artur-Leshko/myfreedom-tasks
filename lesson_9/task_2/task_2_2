document.addEventListener('DOMContentLoaded', function(){
    function showElements(param1, param2, param3) {
        console.log(param1);
        console.log(param2);
        console.log(param3);
    }

    let arrayOfNames = ['Pasha', 'Masha', 'Misha', 'Sasha', 'Natasha', 'Sergei'];
    showElements(...arrayOfNames);
});
